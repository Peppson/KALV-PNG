
on:
  push:
    branches:
      - main

jobs:
  log-message: 
    runs-on: ubuntu-latest
    steps:
      - name: Log a message
        run: echo "eslint.yml"

jobs:
  log-message: 
    runs-on: ubuntu-latest
    steps:
      - name: Log a message
        run: echo "Done bby!"


